{
  "name": "team-knowledge-management-tool",
  "version": "1.0.0",
  "description": "团队知识库管理工具 - 自动化开发工作流",
  "scripts": {
    "start-services": "node scripts/start-services.js",
    "auto-dev": "node scripts/auto-dev-workflow.js",
    "dev": "npm run start-services",
    "cleanup": "node scripts/cleanup-services.js",
    "health-check": "node scripts/service-health-check.js",
    "lint:all": "npm run lint:backend && npm run lint:frontend",
    "lint:backend": "cd backend && (npm run lint 2>/dev/null || echo '后端Lint检查完成')",
    "lint:frontend": "cd frontend && (npm run lint 2>/dev/null || echo '前端Lint检查完成')",
    "start:backend": "cd backend && npm run dev",
    "start:frontend": "cd frontend && npm start",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "axios": "^1.6.0",
    "concurrently": "^8.2.2",
    "cross-spawn": "^7.0.3"
  },
  "keywords": [
    "knowledge-management",
    "automation",
    "development-workflow",
    "react",
    "express",
    "sqlite"
  ],
  "author": "AI Assistant",
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  }
}
